"""Megane GUI (Phase 2): node-graph front-end.

Requires the ``gui`` extra::

    pip install -e ".[gui]"     # PySide6, NodeGraphQt, pyqtgraph

Nothing here is imported by the engine -- ``import megane`` never pulls Qt.
Launch with ``megane gui`` or ``python -m megane.gui``.
"""
